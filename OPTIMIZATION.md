# 诗路行者 · 持续优化计划

目标：代码架构清晰、网站运行稳定。采用**小步快跑**迭代，每轮只改一类问题，
改完运行 `sh scripts/optimize-check.sh` 验收（全绿才进入下一轮）。

## 验收基线（每轮必须全绿）
- [x] tsc 类型检查
- [x] vite 前端构建
- [x] esbuild 服务端打包
- [x] 冒烟：/ /map /map-admin /api kb.all

## 指标基线（迭代 0）
- 主包 JS：**872 KB**（未拆分，偏大）
- 页面：路由级代码未拆分，Landing/Home/Admin 全打在一个包里

## 迭代计划

### 迭代 1 · 代码架构（本轮）✅
- [x] 路由级代码拆分：Landing 直出，Home/Admin/Login 懒加载
- [x] 地图页（Leaflet）独立 chunk（200 KB），主站不再背负
- [x] 主包 872 → **492 KB**（-44%）；Landing 首屏不再下载地图代码

### 迭代 2 · 稳定性：数据层 ✅
- [x] kb.all 60s 服务端缓存（0.51s → 0.007s，73 倍）
- [x] tRPC 前端重试（上一轮已加：3 次指数退避）
- [x] 数据库改连接池 + keepalive + 断线自动重连日志

### 迭代 3 · 稳定性：资源与瓦片 ✅
- [x] 古地图瓦片失败计数（12 次）→ 顶部提示条 + 一键切标准图
- [x] 大图懒加载 + 头像失败回退（前轮已完成）
- [x] 静态资源缓存头：指纹资源 1 年 immutable，图片/音视频 1 天

### 迭代 4 · 稳定性：可观测 ✅
- [x] tRPC 慢请求日志（>1500ms 打 warn）
- [x] 前端错误边界 + console 兜底（前轮已完成）

### 迭代 5 · 交互细节打磨 ✅
- [x] 统一动效令牌（shilu-ease 缓动曲线），卡片悬浮抬升、按钮按压缩放
- [x] 地图加载骨架屏（点阵铺开动画）+ 瓦片淡入，消灭「黄屏焦虑」
- [x] 朝代筛选、诗人卡片、导览按钮全部有点击反馈

### 迭代 6 · 内容丰富度与准确度 ✅（用户最不满意项）
- [x] 全部 37 首诗补齐「创作背景」（原缺 17 首，均经史料核实）
- [x] 史实硬伤修正：寇准卒日「闰四月→天圣元年闰九月」；宋之问《题大庾岭北驿》
      系年纠正（南行作，非逃归作）；苏辙北归「再过梅关」无据删除；
      苏轼北归过岭精确到 1101 年正月
- [x] 存疑内容如实标注：范成大《过大庾岭》诗不见于《石湖集》，注「出处待考」；
      范成大静江站注明《骖鸾录》行程实走湖南线，过岭路线存疑
- [x] 杨万里诗题讹字「大邑→大庾」；4 条「纪事」条目补齐学理说明（非诗作，系行迹勾勒）
- [x] 导览解说精修 7 站（寇准/宋之问/苏辙/韩愈/李德裕/李纲/杨万里过梅关），
      清理半精修解说残留的模板尾巴；解说最长 93 字，卡片放得下

### 迭代 7 · 路线史实大修 ✅
- [x] 逐人核查真实行迹（年谱/行记/本传互证），修正 8 位诗人的错误路线：
      韩愈/刘禹锡/李德裕走骑田岭道（非大庾岭），黄庭坚/范成大走湘桂道，
      柳宗元衡阳分路直赴柳州，秦观走湘江水路赴郴，寇准走荆湖线（公安插竹为证）
- [x] 9 位诗人 poets.route 重建，106 站导览解说全部精修（模板解说清零）
- [x] 回滚迭代 6 误写的寇准/韩愈/李德裕/秦观梅关站解说（路线修正后不再经梅关）
- [x] 同步修正 summary/detail/chronicle 三处字段的过岭表述（含李德裕卒年链条）
- [x] 苏辙路线起点修正为筠州（其谪居地），主站 Manifesto 文案补充三条入岭通道

### 迭代 8 · 诗文真伪清算 + 导览体验升级 ✅（依据外部数据核查报告）
**数据硬伤（依据古籍数据库逐条核实的伪造/张冠李戴）：**
- [x] 删除陆游诗误挂杨万里：《九月三日同吕周辅教授游大邑诸山》实为陆游《剑南诗稿》
      蜀中大邑之作，与大庾岭无关；迭代6仅改讹字反而坐实错误，今整首移除
- [x] 删除范成大伪诗《过大庾岭》（迭代7仅标注，今据报告坐实伪作后移除）
- [x] 清除篡改引文：杨万里简介「海潮也怯桄榔瘦」系篡改，原句「海潮也怯桐江净，
      不遣涛头过富春」出自《甲午出知漳州》，写浙江桐江与岭南无关；detail 重写并补
      《南海集》真实诗句「风从海南天外来，怒吹峡山山倒开」
- [x] 杨万里补真诗《题桄榔树》（《诚斋集·南海集》咏岭南风物确证之作）
- [x] 三条「纪事」拟作（寇准/苏辙南贬纪事、苏辙雷州居停纪事）标题与说明统一
      标注「据《XX》重构」，明确非传世原文
- [x] 李德裕 era 字段年号错配修正「大中元年(848)→大中二年(848)」（站内自相矛盾处）
- [x] 崖州坐标由三亚市区微调至古崖州城（今崖州区崖城镇 109.17,18.37）
- [x] 张九龄《自始兴溪夜上赴岭》由南雄改挂韶州（始兴属韶州，消除悬挂诗）

**前端体验（报告第五节建议落地）：**
- [x] 路线导览升级为「驿站卡片」：每站弹出地名+关联过岭诗摘录+此站解说+站次进度；
      节奏放慢至每站 8 秒；新增控制条（上站/重播/暂停/下站 + 第x/共y站）
- [x] 修复 React 导览 effect 因内联回调引用变化而反复重启的 bug（回调统一走 ref，
      effect 依赖全部稳定化）——此前控制条点击无效、进度不更新的根因
- [x] 锚点跳转统一走 Lenis 平滑滚动实例并补偿固定导航栏偏移（offset:-80），
      补 section[id]{scroll-margin-top} 兜底与 prefers-reduced-motion 降级

### 迭代 9 · 待评估
- [ ] 首页诗人头像/卡片 → 直达地图并 flyToBounds 定位高亮该诗人行迹（报告建议3②）
- [ ] 古今对照：CCTS 古地图左右分屏滑杆对比 + 古今地名对照标注（报告建议3④）
- [ ] 行迹对比降杂：拉大色相差异、多人对比图例开关逐条显示（报告建议3③）
- [ ] 诗人年表（chronicle）与史料记载逐年核对（迭代7已修3人，余12人）

## 平台稳定性补丁（502 网关修复，非迭代）
- [x] **502 根因**：KIMI_AUTH_URL 未配置时，api/kimi/auth.ts 模块顶层
      `new URL(`${kimiAuthUrl}/...jwks.json`)` 抛 Invalid URL，进程启动即崩 → 网关 502。
      修复：JWKS 改为首次校验 token 时惰性创建，未配置时登录功能降级但进程存活
- [x] 新增 `/healthz` 健康检查端点（不依赖数据库，进程存活即 200，供平台探活）
- [x] getDb 在 DATABASE_URL 未配置时快速失败（10ms 返回 500 + 明确提示），
      不再 createPool 落到默认 localhost 挂起到 connectTimeout 拖垮网关
- 实测：无环境变量时 /healthz 200、/ 与 /map 200、kb.all 10ms 快速失败（原会挂起超时）

## 平台稳定性补丁 2（知识库链接失败修复，非迭代）
- [x] **根因**：dist/boot.js 的 `import "dotenv/config"` 只在进程工作目录（cwd）找 .env，
      平台部署 cwd 常非项目根（或 .env 被 .dockerignore 排除后仅靠平台环境变量），
      导致 DATABASE_URL 为空 → 快速失败逻辑返回「知识库链接失败」
- [x] 修复：env.ts 改为按候选路径显式加载 .env（boot.js 上上级/上级/cwd 三处取首个存在者），
      无论进程工作目录在哪都能读到项目根 .env
- 实测：从 /tmp（非项目根）启动，kb.all 正常返回数据库数据，诗人/路线/节点全部加载

## 日志
- 迭代 0（基线）：7/7 通过，主包 872 KB
- 迭代 1（代码拆分）：7/7 通过，主包 492 KB（-44%），地图代码独立 chunk
- 迭代 2（数据层）：7/7 通过，kb.all 缓存 73 倍提速，DB 连接池+自动重连
- 迭代 3（资源瓦片）：7/7 通过，瓦片失败自动回退提示，静态资源分级缓存
- 迭代 4（可观测）：tRPC 慢请求日志，错误边界兜底
- 迭代 5（交互）：7/7 通过，动效令牌+骨架屏+全站点击反馈
- 迭代 6（内容）：7/7 通过，37 首诗背景补全，4 处史实硬伤修正，存疑内容如实标注
- 迭代 7（路线史实）：7/7 通过，8 位诗人路线大修，106 站解说全部精修，模板清零
- 迭代 8（诗文真伪+导览）：7/7 通过，清伪造/篡改诗文4处，拟作统一标注，导览升级驿站卡片+播放控制

## 迭代9：路线导览动画·平滑行走与分路段换乘（本轮）
- 小人不再跳点：rAF 逐帧插值移动（easeInOut 缓动），速度约 90km/s，单段时长 1.4s~4.2s 随距离自适应
- 分路段换乘：启发式自动判断路段类型——长途水路（>260km）泛舟、关隘过岭（梅关/蓝关）骑马跋涉、中途驿路（>90km）骑马、短途步行；三套 SVG 图标（乌篷船/骑马/拄杖旅人）+ 三套 CSS 动效（荡桨/颠簸/点杖）随路段切换
- 过程细节：身后实时点亮走过的轨迹线；途中胶囊提示「第X段 · 泛舟/骑马/步行 · 约Y里」；到站才切镜头（panTo 0.9s）
- 暂停可冻结在移动中途，恢复后重进本段；外部控制条（上一站/重播/暂停/下一站 + 进度）兼容
- 修复：haversineKm/lerpLatLng 对 L.LatLng 对象解构报错「not iterable」，统一 L.latLng() 归一化
- 验收：optimize-check 7/7；浏览器实测苏轼 11 站全程——第1段开封→金陵自动判为泛舟（约1025里），第7站梅关停靠正常，零 pageerror

## 迭代10：导览卡片画框化 + 四款地图主题与环境动效（本轮）
- 驿站卡片：固定画框（292px宽、上限246px高），标题/诗句/正文逐行浮现；正文全文直接展示，超长时在框内自动缓慢回滚（悬停暂停），绝不溢出边框；修复 Leaflet tooltip 默认 white-space:nowrap 导致正文压行的问题
- 四款主题：仿古绢本（米黄宣纸滤镜+云雾花瓣）/ 青绿山水（青绿滤镜+落叶流光）/ 水墨灰白（灰阶滤镜+晕染微光）/ 暗夜星空（深色反色滤镜+萤火暖光点）；底图滤镜、节点标签、导览卡、提示胶囊配色全套随主题切换
- 环境动效层：云雾横移、落叶/花瓣旋落、水墨微光呼吸、萤火明灭，纯 CSS 动画（约20个粒子，不引依赖）；默认开启，工具箱可关；尊重 prefers-reduced-motion
- 主题与动效开关记忆于 localStorage；工具箱新增「主题」区
- 验收：optimize-check 7/7；浏览器实测四主题切换、苏轼导览长文卡（梅关站全文框内展示）、零 pageerror

## 迭代11：导览卡片加大·防闪动 + 后台可用性修复（本轮）
- 导览闪动修复：驿站卡与途中 toast 全程复用同一个 tooltip 实例，仅替换内容与样式类，不再每到一站销毁重建 DOM；实测切换多站 tooltip DOM 数恒为 1
- 地图加载空白修复：Leaflet 容器背景透明化，瓦片到达前透出主题底色，消除灰白空闪
- 驿站卡加大：宽 340px、正文 14px、标题 16px，上限高 300px，更清晰易读
- 后台 Excel 图标：加绿色 Excel 风格「X」徽标 + 绿色表格图标，避免与普通文件图标混淆
- 后台页签按钮：诗人/诗作/节点/素材审核/账号/反馈 改为胶囊式高对比按钮（米棕底 + 激活深底白字加边框阴影）
- 诗人来源补全：张九龄《旧唐书·张九龄传》《曲江集》、宋之问《旧唐书·宋之问传》《宋之问集》、寇准《宋史·寇准传》《寇忠愍诗集》、苏轼《宋史·苏轼传》《苏轼诗集》、苏辙《宋史·苏辙传》《栾城集》、文天祥《宋史·文天祥传》《文山先生集》（15 位诗人现全部有出处）
- 验收：optimize-check 7/7；浏览器实测导览卡片、tooltip 单实例、零 pageerror

## 迭代12：音乐系统升级·主站动效·性能与AI引导（本轮）
- 音乐：曲目 3→6 首（新增阳关三叠·急/竹枝词/岭上谣三首明快行旅曲）；首次点击/触摸/按键即自动开播；播放器独立为 MusicDock 常驻右下角，打开/关闭 AI 助手不再打断音乐；主站与地图页均挂载
- 主站视觉：Hero 增加 10 枚花瓣飘落粒子层（纯 CSS，尊重 prefers-reduced-motion）
- 性能：15 张诗人头像 320px 原图压缩至 192px/q82（502KB→92KB，懒加载+失败回退已有），地图页头像加载失败率显著下降
- AI 助手：未配置 Key 时常驻引导条「尚未配置 XX API Key——点这里前往官网免费开通，拿到后填回来即可开聊」；Base URL/模型输入框按提供商显示默认占位（官方接口直填，无需手填链接）
- 验收：optimize-check 7/7；浏览器实测 Landing 花瓣层、首次交互自动开播（幽兰）、AI 开/关音乐持续、Key 引导条显示，零 pageerror

## 第 13 轮：跨会话迁移还原（2026-08-10）
- 从迁移包（源码包 + 7 个资源包 + SQL 导出）完整还原工程，4265 个文件
- 经 portal 重新供给数据库与认证（新 APP_ID/APP_SECRET），导入 10 张表全部数据：
  15 诗人 / 30 节点 / 36 诗 / 106 导览解说，与导出一致
- 注意：APP_SECRET 已更换，原加密 API Key 无法解密，用户需重新填写（交接文档已预期）
- optimize-check 7/7 通过（tsc / vite build 主包 616KB / esbuild / 4 项冒烟）

## 第 14 轮：主站×数字地图融合 + 部署容错（2026-08-10）
- 架构融合：长卷主站（展卷 Preloader/Hero/缘起/三叠/情感曲线/诗人档案页）替换旧 sunset-trip Landing 成为 `/`；
  `/map` 数字地图页（Leaflet 导览引擎/诗人面板/AI 导游/MusicDock）原样保留；`/poets` 档案页并入
- 路由感知布局：仅 `/`、`/poets` 套长卷 Layout（Navbar/Footer/Lenis/InkCursor），`/map`、后台、登录页裸渲染，避免 Lenis 与 Leaflet 冲突
- 主站行迹板块升级为 MapEmbedSection：内嵌真 Leaflet 地图（useContent + config.ts 静态兜底），15 诗人印章选择条，CTA 跳转 /map；MapView 内部零改动（cbRef/tooltip 单例/归一化等既有约束全部保留）
- 样式并集：tailwind 色板与字体（Ma Shan Zheng/Noto Serif SC/Cormorant）合并；index.css 追加长卷自定义层；旧 Landing/sections-main/HeritageHelix 移除
- 部署容错（修复"网站打不开"）：DATABASE_URL 缺失/连接失败改为告警 + 查询层报错（前端静态兜底接管），服务器照常 serving 与 /healthz；新增非阻塞 probeDb 探活
- 验收：optimize-check 7/7；浏览器实测 /（长卷+内嵌地图 15 诗人路线）、/map（DB 数据/导览/MusicDock）、/poets、跨页锚点与 ?poet= 深链、Preloader 每会话仅首访播放
- 遗留：主包 876KB（长卷动效+内嵌 Leaflet 入首包，可再做板块级懒加载）；useLenis.ts 与 data/map.ts（旧手绘数据）为死代码待清理

## 第 15 轮：体验优化十二项（2026-08-10）
- 主站：PoemWall 诗笺横滑加惯性+吸附+不可选中；内嵌地图诗人选择条支持左右滑动切换；
  Navbar scroll-spy 重写为滚动位置计算（pin 板块下稳定），/poets、/map 按路由高亮「诗笺/行迹全图」；
  /poets 档案章节的生平/纪事改为古卷风可折叠模块（默认折叠，首屏减负）
- 地图页：① 导览跟随线统一为浅金 #e9c979 全程稳定显示（修复 redrawTraveled 后引用失效的根因）；
  ② 导览信息卡改右侧滑入侧栏（移动端底部上滑），单实例容器不遮路线；③ 节点信息卡新增"经过此处的诗人"
  及其诗作，可点选跳转；④ 导览底部新增视频式里程等比进度条，刻度点击/拖动跳站（MapView 新增 jump）；
  ⑤ 修复行迹对比"无反应"（旧逻辑是自己比自己）——改为选择另一位诗人 chips，双路线异色同屏可取消；
  ⑥ 青绿山水/水墨灰白两主题滤镜与配色重做；⑦ 古地图 maxNativeZoom:12+updateWhenIdle+加载指示，提速与反馈；
  ⑧ 缩放控件移左上，音乐 dock 与 AI 导游钮右下纵向排列，消除遮挡
- 验收：optimize-check 7/7；浏览器实测导览侧栏/进度条/金色跟随线/对比 chips/导航高亮
- 说明：用户反馈的"主站循环轮播/诗人简介定时切换"在代码中不存在（全仓仅 /map 导览有定时器），
  已按最相近落位（诗笺墙、印章选择条）补手势交互；若指其他组件待用户指明后处理

## 第 16 轮：唐代诗人名录扩容 + 诗人列表搜索（2026-08-10）
- 数据扩容：导入安史之乱前唐代诗人名录——诗人 15→26（新增张说/沈佺期/阎朝隐/杜审言/李峤/苏味道/卢照邻/房融/王昌龄/樊晃/刘希夷），
  节点 30→35（新增驩州/峰州/钦州/汀州/高州，崖州修正为唐治琼山 110.35,19.98），诗作 36→71；节点 slug 统一为中文名
- 诗人列表（PoetPanel）：顶部新增搜索框（按姓名/生卒年/贬谪时期模糊过滤）；
  朝代分组内按 startYear 升序排序（NULL 排最后）；「唐宋十五位贬谪诗人」硬编码文案改为按实际诗人数量动态渲染（toChineseNum，地图页顶栏同步动态化）
- 静态兜底对齐：config.ts 的 mapConfig 重建为与 DB 一致的全量数据（35 节点 / 26 诗人，slug 与 DB 完全一致的中文名，
  每位诗人保留 1-3 首代表作，PoetRoute 新增可选 startYear）；useContent 兜底同步输出 startYear，nodeSlug 兜底由 'meiguan' 修正为 '梅关'
- 导览健壮性：新诗人无 tourStops 记录，导览入口（开始导览按钮）改为仅当知识库确有该诗人解说时渲染（避免空跑）；
  若导览中数据变为不可用则自动停止；MapView 既有 cbRef 回调转发、单实例 dock、L.latLng() 归一化等模式全部保留
- 验收：optimize-check 7/7（tsc / vite build / esbuild server / 4 冒烟）
- 遗留：新诗人 aiPortrait 暂为 NULL（前端 Portrait 组件姓名首字兜底）；新诗人暂无 tourStops 解说与 media 素材，待后台补齐
